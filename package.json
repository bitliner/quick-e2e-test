{
	"name": "quick-e2e-test",
	"version": "1.0.9",
	"description": "It allows you to build end-to-end test specifying just the scenarios to test in natural language (text).",
	"main": "quick-e2e-test.js",
	"scripts": {
		"test": "cd test && mocha ./test.js",
		"generate-parser": "cd lib && cd parser && pegjs ./grammar.pegjs ./parser.js"
	},
	"bin": {
		"quick-e2e-test": "bin/quick-e2e-test"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"ejs": "^2.3.1",
		"jformatter": "^1.0.8",
		"logb": "0.0.2"
	},
	"devDependencies": {
		"chai": "^2.3.0"
	}
}
